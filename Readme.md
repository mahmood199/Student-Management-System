# RUN USING DOCKER

## Build image
```
docker build -t <image-name> .
```
## Run container
```
docker run -d -p <image-name>
```
## Stop container
```
docker stop <container-id>
```
## Remove container
```
docker rm <container-id>
```
